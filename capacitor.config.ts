import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plannig-poker-ionic-app',
  webDir: 'www'
};

export default config;
